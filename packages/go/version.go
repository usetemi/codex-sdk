package codexsdk

// Version is the SDK package version. It tracks the stable Codex version targeted by this package.
const Version = "0.130.0-6"

// TargetCodexVersion is the Codex CLI version this package targets.
const TargetCodexVersion = Version
